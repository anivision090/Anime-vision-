import React from "react";
import ReactDOM from "react-dom/client";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
    <h1 style={{ color: "white", textAlign: "center" }}>
      Delwa React App Started ✅
    </h1>
  </React.StrictMode>
);
